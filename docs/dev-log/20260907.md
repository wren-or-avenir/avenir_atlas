# 2026-09-07

## 今日完成

- 海洋视觉进行新一轮重构，目标从抽象光带调整为更接近参考图的真实海面：
  - 顶点着色器改为多频方向波叠加，加入波面斜率与更高的 256×256 网格细分；
  - 片元着色器加入反照率纹理、双层 UV 漂移、细节法线、镜面高光和基于 Voronoi/湍流的连续泡沫；
  - 白天使用 `ocean_day_09.jpg` 的蓝色海面与白蓝浪花方向，夜间保留深蓝海面和蓝眼泪荧光；
  - 夜间荧光集中在浪脊、泡沫和动态细节上，降低均匀像素噪声感。
- 新增海洋反照率生成脚本：从参考图生成可平铺的 `1024×1024` 纹理和均值元数据，运行时加载失败会回退为程序化着色。
- 新增开发期 `lil-gui` 调参面板，可调整泡沫阈值、泡沫样式、波层数量、波尺度、细节法线、高光和反照率强度；生产构建不加载该面板。
- 更新海洋颜色测试和开发依赖，补充 `jpeg-js`、`pngjs`、`lil-gui` 类型声明。
- 参考图文件重新命名为 `ocean_current_look_01.png`，并加入 `ocean_current_look_02.png`，用于记录当前视觉效果。

## 当前状态与限制

- 海洋效果仍处于运行时视觉验收和参数微调阶段，不能仅以类型检查或构建通过代替验收。
- 当前入口暂时强制使用白天主题，便于集中调整日间海水；恢复自动昼夜切换前需要完成白天和夜间两套效果验收。
- 鼠标推浪暂时关闭，相关 wake 数据结构和着色器路径仍保留，待基础海浪形态稳定后重新启用。
- 地图菜单、天气图层、月相滚动条、加载图标和 Notion 内容链路仍未实现。

## 明日计划

- 运行页面检查白天海水是否具备连续的波峰、浪谷、泡沫网和自然反光。
- 以 `ocean_day_09.jpg`、`ocean_night_11.jpg`、`ocean_night_12.jpg` 为基准微调参数，并记录验收截图。
- 海洋视觉稳定后，再恢复鼠标推浪并开始 2D 世界地图菜单。
